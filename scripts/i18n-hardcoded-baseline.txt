# i18n hardcoded string baseline — regenerated 2026-07-15
# All 222 historical matches RESOLVED. This file is intentionally empty
# so that `scan-hardcoded-strings.sh --baseline <this-file> --strict`
# will block ANY new hardcoded string from being introduced.
#
# Format: one match per line as `file:line:content` (no leading comment lines
# are treated as matches — the scanner filters non-matching lines).
